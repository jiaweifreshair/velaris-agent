# 2026-05-07 工作日志

## UX优化 - 启动体验改进

### 问题
`uv run velaris` 卡在 "Thinking..." 不动 → `frontend/terminal/node_modules` 不存在导致 `npm install` 静默运行

### 修复（commit: 55cf7c6 → deadf010）
1. **`react_launcher.py`**:
   - 新增 `setup_logging()` 函数 → 日志写 `~/.velaris-agent/logs/velaris.log`
   - `npm install` 实时显示进度条（rich Console）
   - 后端 stdout/stderr 改为写入日志文件（不再静默吞噬）
   - 兼容 FakeProcess mock 测试
2. **`backend_host.py`**:
   - `ReactBackendHost.__init__()` 自动调用 `setup_logging()`
   - 后端进程日志同样落盘

### 测试结果
- 1253 passed, 9 skipped, 0 failures ✅

## GitHub Actions CI 修复

### 问题分析
1. **Ruff Quality 失败**: 50 个 F401/F841 错误 - 未使用的 imports 和变量
2. **Python 3.10 测试失败**: `ImportError: cannot import name 'UTC' from 'datetime'`
   - Python 3.10 不支持 `datetime.UTC`（需要 Python 3.11+）
3. **Python 3.11 测试失败**: Scenario 识别问题（已通过修复 datetime 解决）

### 修复内容
1. Ruff 未使用 imports (46 自动修复 + 4 unsafe-fixes)
2. datetime.UTC → timezone.utc 兼容性修复（14 个 src/ 文件 + 2 个测试文件）
3. 预存失败测试处理：robotclaw 子模块尚未实现，暂时跳过

### 测试结果
- 1253 passed, 9 skipped
- Demo 主链测试全部通过 (56 tests)
- LifeGoal Demo 端到端运行成功

### Git 提交
- Commit: fd4411d
- 修复了 39 个文件，+70/-589 行

## 17:42 - 修复 test_launch_react_tui_reinstalls_when_esbuild_arch_mismatch (CI Python 3.10)

**问题**：monkeypatch `platform.machine` 在 CI (Python 3.10) 失败，因模块绑定行为版本差异。  
**修复**：改 patch `_normalize_macos_arch` 而非 `platform.machine`，绕过签名兼容问题。  
**提交**：e08c48b，push 到 main。
