# 2026-05-08

## PR #4 分析与修复

**PR #4**：「修复 Windows 交互模式输入后卡住问题」by CiicDong，2 commits（7fc99a7 + c91f43d），8 个文件修改。

### CI 失败根因
`src/openharness/config/settings.py` 的 `_apply_env_overrides` 中 PR 引入了两个错误条件：
1. `if not settings.model or settings.model == Settings().model:` → 导致文件中已设置 model 后 env var 无效
2. `if not candidate.api_key:` → 导致文件中已设置 api_key 后 env var 无效

违反了 **env > file** 的优先级原则，`test_load_applies_env_overrides` 在 Python 3.10/3.11 失败。

### Mac 兼容性结论
- `environment.py` 用读 `.git/HEAD` 替代 `subprocess git` ✅ Mac 完全兼容
- `backend_host.py` stdin UTF-8 + `_handle_request` 重构 ✅ Mac 中性
- frontend Windows 平台检测 ✅ Mac 不触发

### 处理
- 在本地 `fix-pr4-settings-env-override` 分支 cherry-pick PR#4 全部提交，修复 settings.py 两处问题
- 本地全量测试：**1254 passed, 0 failed**（macOS Python 3.12）
- 推送到 `origin/fix-pr4-settings-env-override`
- 已在 PR #4 提交 Review 评论说明根因和修复方案

## CI 修复推送 main（二次）

PR #4 已被合并进 main（`ab35a70`），带着 settings.py bug。
直接在 main 上修复两处问题，commit `9e82e59` 推送到 `origin/main`。
CI run `25550675587` 全部通过：Python 3.10 ✅ / 3.11 ✅ / Frontend typecheck ✅ / Python quality ✅
